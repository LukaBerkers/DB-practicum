-- Alle uitgevers die uitsluitend spellen in het Nederlands gepubliceerd hebben.

SELECT pubid, pname FROM Publisher
WHERE pubid NOT IN (
    SELECT pubid
    FROM Publishes
    NATURAL JOIN Version
    NATURAL JOIN InLanguage
    WHERE langcode <> 'dut'
);
